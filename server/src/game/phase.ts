enum Phase {
    IDLE,
    CHOOSE_DECK,
    BEGIN_TURN_STEP,
    UNTAP_STEP,
    START_TURN_STEP,
    DRAW_STEP,
    CHARGE_STEP,
    MAIN_STEP,
    ATTACK_STEP,
    END_STEP,
    EOT_TRIGGERS
}

export default Phase